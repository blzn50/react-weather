import React from 'react';

const Header = props => (
  <div>
    <h1 className="title">Weather Finder</h1>
    <p className="subtitle">Finding weather at your your service.</p>
  </div>
);

export default Header;
